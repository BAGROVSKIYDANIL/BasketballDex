import { Layout } from "../common/components/ui/layout/layout";
import AddTeam from "../modules/teams/components/TeamAdd/AddTeam";

const TeamAdd = () => {
    return (
        <Layout>
            <AddTeam/>
        </Layout>
    );
};

export default TeamAdd;